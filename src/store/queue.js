import { defineStore } from "pinia";

export const useQueueStore = defineStore({
  id: "queue",
  state: () => {
    return {
      queues : [
           
      ]
    }
  },
  actions: {
    
  }
});
